export type ProductData = {
  category?: string;
  name: string;
  priceValue: number;
  priceCurrency?: string;
  brand: string;
  operatingSystem: string;
  color: string;
  model: string;
  SKU: string;
  memory: string;
  yearRelease?: number;
  photoUrl?: string;
  photoFilename: string;
  photoAlt: string;
  screenSize: string;
};
