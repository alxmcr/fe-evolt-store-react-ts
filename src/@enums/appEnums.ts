export enum LoadingStates {
  IDLE = 'IDLE',
  PENDING = 'PENDING',
  SUCCESS = 'SUCCESS',

  ERROR = 'ERROR',
}

export enum TagFilterKeys {
  BRAND = 'filter-brand',
  STORAGE = 'filter-storage',
  BLUETOOTH = 'filter-bluetooth',
}
